import React from "react";

const AddRecordedActivityPanel = () => {
  return (
    <div className={`${css["rec-activity-panel"]}`}>
      <div className={`${css["panel-header-wrapper"]}`}>
        <div className={`${css["panel-header"]}`}>Add Activity</div>
      </div>
    </div>
  );
};

export default AddRecordedActivityPanel;
